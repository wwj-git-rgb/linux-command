interface:
  display_name: "Linux Command"
  short_description: "Repo-specific patterns and workflows for linux-command"
  default_prompt: "Use the linux-command repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true